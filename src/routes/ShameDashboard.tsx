/**
 * Shame dashboard. Phase 0 placeholder — aggregates everyone currently marked
 * absent across the competition's groups starting in Phase 4.
 */
export default function ShameDashboard() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold">Dashboard</h2>
      <p className="mt-2 text-sm text-slate-500">
        Staff who aren't doing their assignments will show up here.
      </p>
    </div>
  );
}
